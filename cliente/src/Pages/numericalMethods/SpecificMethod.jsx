import Metodo from '../../Components/Metodo'

const specificMethod = ({title, icon, id}) => {

  return (
    <>
      <Metodo title={title} icon={icon} id={id}/>
    </>
  )
}

export default specificMethod